version: '3.8'

services:
  app:
    build: ./app
    ports:
      - 80:5000
    links:
      - percona-db

  percona-db:
    build: ./db
    restart: always
    environment:
      MYSQL_DATABASE: 'test'
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306
